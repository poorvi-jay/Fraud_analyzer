VITE_API_BASE_URL=http://localhost:8000

# Phase 2: reviewer sign-in (Supabase Auth). Same project as the backend's
# SUPABASE_URL -- the anon key is safe to ship to the browser, it only ever
# grants what your Supabase RLS policies allow (see supabase/schema.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
